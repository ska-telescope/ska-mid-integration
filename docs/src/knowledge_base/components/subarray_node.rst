.. _components_sn:

==============
Subarray Node
==============


TRL
==============
`mid-tmc/subarray-node/1`

.. note:: In the full scale deployment, there are 16 instances of Subarray Node.
   In such case, only the last part of the TRL indicates the id of the subarray.
   E.g. `mid-tmc/subarray-node/2`, `mid-tmc/subarray-node/3` and so on.

Attributes
==============

.. csv-table:: Attribute List
   :file: data/subarray_node.csv
   :header-rows: 1
